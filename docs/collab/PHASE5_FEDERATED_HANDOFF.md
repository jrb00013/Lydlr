# Phase 5 — Federated Fleet Learning (handoff)

**Owner:** Other AI (Phases 1–2 agent) + Cursor (integration)  
**Status:** Ready to start after Phases 3–4 land  
**Coordination:** deepiri-polylogue journal + Redis task queue

## Context from Cursor (Phases 3–4 complete)

Phases 3 and 4 added:

- `modality_codec.py` — LYMS framing, mel audio, LiDAR downsample, IMU delta
- Edge compressor modality gates + per-modality metrics in `wire.MetricsPayload`
- `trt_inference_node` + deploy API `inference_backend` (torch/onnx/trt)
- Scripts: `build_tensorrt_engine.sh`, `validate_deploy_bundle.py`, `bench_inference_backend.py`

Phases 1–2 (other AI) should deliver:

- Closed-loop link policy bench + LPIPS guard
- RL `CompressionController` with metrics fields `controller_mode`, `action`, `reward`

## Phase 5 scope (20 commits target)

### 5A — Federated core (ROS)

1. Replace `federated_sync_node.py` stub with FedAvg over LoRA/gradient deltas
2. Cap delta size using `uplink_budget_kbps` from `NodeLinkPolicy`
3. Sign deltas (sha256) before uplink; verify on coordinator

### 5B — Backend

4. Mongo collection `federated_rounds`
5. `POST /api/federated/rounds/` — start round, list participants
6. `GET /api/federated/rounds/{id}/` — status, merged version
7. Wire coordinator idle trigger when fleet `estimated_throughput_kbps` < 50% budget

### 5C — UI

8. Federated tab: round timeline, participating nodes, merged artifact version
9. Export round report CSV

### 5D — Integration contracts (do not break)

- Use existing `MetricsPayload.modality_bytes_out` for round cost accounting
- Deploy merged weights via existing `DeploymentView` + `inference_backend`
- Journal handoff: append `type=handoff` events to polylogue journal

## API sketch

```json
POST /api/federated/rounds/
{
  "participant_node_ids": ["node_0", "node_1"],
  "base_version": "vv1.0",
  "max_delta_kbps": 128
}
```

## Files to touch

- `ros2/src/lydlr_ai/lydlr_ai/model/federated_sync_node.py`
- `backend/api/views/federated_views.py` (new)
- `backend/api/repositories/federated_repository.py` (new)
- `frontend/src/components/FederatedView.js` (new)

## Dependencies

| From Phase | Needed for Phase 5 |
|------------|-------------------|
| Phase 1 link health API | Coordinator idle detection |
| Phase 2 RL metrics | Optional reward signal for round scheduling |
| Phase 3 modality metrics | Uplink cost per round |
| Phase 4 TRT deploy | Push merged ONNX/TRT bundle |

## First task for other AI

Implement `federated_sync_node` FedAvg skeleton + `POST /api/federated/rounds/` stub with tests.

---

*Generated by Cursor agent executing Phases 3–4. Sync via polylogue journal.*
